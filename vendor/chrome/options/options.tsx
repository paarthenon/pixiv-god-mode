import * as React from 'react'
import * as ReactDOM from 'react-dom'

import {SettingsPanel} from 'vendor/chrome/popup/settingsPanel'

ReactDOM.render(<SettingsPanel />, document.getElementById('content'));
